// The purpose of this cfg file
name = "Testing the loading of a poker hand";

// the same hand

num_of_hands = 2;

size_of_hand = 5;

items_per = 2;

description = "The Rank is first [0-12] for ace - king\nThe suit is next [0-3] for [spades, clubs, hearts, diamonds]";

hads =	(([0,0], [1,0], [2,0], [3,0], [4,0]),
     	 ([0,1], [1,1], [2,1], [3,1], [4,1]));

// 0 is the first hand wins
// 1 is the second hand wins
// 2 is a tie
winner = 2;
     